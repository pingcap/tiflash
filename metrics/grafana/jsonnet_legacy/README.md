# Archived jsonnet sources

This directory holds the pre-grafanalib TiFlash Summary dashboard sources
(jsonnet + grafonnet-lib helpers).

**Do not use these files to generate dashboards.** The active source of truth is:

- `../tiflash_summary.dashboard.py`
- `../common.py`
- `../generate_dashboard.sh`

This archive will be removed in a follow-up cleanup PR.
