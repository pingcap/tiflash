>> drop table if exists test
>> create table test (
	dt Date
	) engine = TxnMergeTree((dt), 8192, '')

>> desc test
┌─name─┬─type─┬─default_type─┬─default_expression─┐
│ dt   │ Date │              │                    │
└──────┴──────┴──────────────┴────────────────────┘

>> drop table if exists test
