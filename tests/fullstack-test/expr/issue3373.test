mysql> drop table if exists test.t,test.t2 ;
mysql> create table test.t(a int) ;
mysql> create table test.t2(a double, b float);
mysql> alter table test.t set tiflash replica 1;
mysql> alter table test.t2 set tiflash replica 1;

func> wait_table test t

func> wait_table test t2

mysql> select sum(a) as x from test.t  having x not in (select b from test.t2 where x in (b));
+------+
| x    |
+------+
| NULL |
+------+
