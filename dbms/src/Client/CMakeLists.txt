# TODO: make separate lib datastream, block, ...
#include(${TIFLASH_SOURCE_DIR}/cmake/dbms_glob_sources.cmake)
#add_headers_and_sources(tiflash_client .)
#add_library(tiflash_client ${SPLIT_SHARED} ${tiflash_client_headers} ${tiflash_client_sources})
#target_link_libraries (tiflash_client tiflash_common_io ${Poco_Net_LIBRARY})
#target_include_directories (tiflash_client PRIVATE ${DBMS_INCLUDE_DIR})
